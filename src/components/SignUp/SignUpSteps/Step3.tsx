import React from 'react'

type Props = {
  handleNext: () => void
  handleBack: () => void
}

export default function SignUp3(props: Props) {
  const { handleNext, handleBack } = props
  return <div>SignUp3</div>
}
